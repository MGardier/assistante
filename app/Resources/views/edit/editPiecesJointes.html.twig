{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('css/formulaires/creerChambre.css') }}">
    {{ parent() }}
{% endblock %}
{% block body %}
    <h1>Modifier un nouveau fichier</h1>
    <div class="container">
        <div class="formulaire">
            {{ form_start(form) }}
            <div class="form-group">{{ form_row(form.oldName, {'label': 'Nouveau nom'}) }}</div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock body %}